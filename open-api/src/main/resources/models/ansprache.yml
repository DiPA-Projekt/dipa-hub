Ansprache:
  type: object
  properties:
    anrede:
      description: Anrede
      type: string
      example: "Herr"
    titel:
      description: Titel
      type: string
      example: "Dr."
    vorname:
      description: Vorname
      type: string
      example: "Max"
    nachname:
      description: Nachname
      type: string
      example: "Mustermann"
    firma:
      description: Firma
      type: string
      example: "Muster GmbH"
