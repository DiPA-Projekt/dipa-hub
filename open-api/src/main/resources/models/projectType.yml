ProjectType:
  type: object
  properties:
    id:
      description: Unique identifier of the projectType
      type: integer
      format: int64
      example: 2
    name:
      description: The name of the project type
      type: string
      example: "Softwareneuentwicklung"
  required:
    - id
    - name