OperationType:
  type: object
  properties:
    id:
      description: Unique identifier of the operationType
      type: integer
      format: int64
      example: 2
    name:
      description: The name of the operation type
      type: string
      example: "Softwareneuentwicklung"
  required:
    - id
    - name