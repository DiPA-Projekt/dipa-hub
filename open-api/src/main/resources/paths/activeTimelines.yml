get:
  tags:
    - timelines
  summary: active timelines list
  description: Retrieves the list of active timelines.
  operationId: getActiveTimelines
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../models/timeline.yml#/Timeline"
