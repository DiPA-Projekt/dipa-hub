get:
  tags:
    - projectFlow
  summary: Project Flow
  description: Retrieves the list of available project flow steps.
  operationId: getProjectFlow
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../models/projectFlowStep.yml#/ProjectFlowStep"
