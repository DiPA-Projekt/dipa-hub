patch:
  tags:
    - milestones
  summary: Update status for a milestone
  description: Apply an operation for a milestone
  operationId: updateMilestoneStatus
  parameters:
    - in: path
      name: timelineId
      schema:
        type: integer
        format: int64
      required: true
      description: Numeric ID of the timeline.
    - in: path
      name: milestoneId
      schema:
        type: integer
        format: int64
      required: true
      description: Numeric ID of the milestone.
  requestBody:
    description: operation type with parameters
    required: true
    content:
      application/json:
        schema:
          $ref: "../models/milestones.yml#/Milestone"
          # type: object
          # properties:
          #   statusId:
          #     type: integer
          #     format: int64
  responses:
    204:
      description: The update was applied successfully.