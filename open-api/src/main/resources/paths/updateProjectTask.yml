patch:
  tags:
    - projectTasks
  summary: Update data for projectTask
  description: Update new data for projectTask
  operationId: updateProjectTask
  parameters:
    - in: path
      name: timelineId
      schema:
        type: integer
        format: int64
      required: true
      description: Numeric ID of the timeline.
    - in: path
      name: projectTaskId
      schema:
        type: integer
        format: int64
      required: true
      description: Numeric ID of the projectTask.
  requestBody:
    description: projectTask as requestBody
    required: true
    content:
      application/json:
        schema:
          $ref: "../models/projectTask.yml#/ProjectTask"
  responses:
    204:
      description: The update was applied successfully.
