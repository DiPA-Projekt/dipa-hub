get:
  tags:
    - timelines
  summary: Timeline List
  description: Retrieves the list of available timelines.
  operationId: getTimelines
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../models/timeline.yml#/Timeline"
