secretGenerator:
  - name: database-password
    literals:
      - password=dipasecret
resources:
  - sa.yaml
  - role.yaml
  - rb.yaml
  - dipa-storage.yaml
  - dipa-db-itzbund-deployment.yaml
  - dipa-db-ba-deployment.yaml
  - dipa-hub-config.yaml
  - dipa-hub-deployment.yaml